{# src/BlogDemoBundle/Resources/views/BlogDemo/formulaire.html.twig #}

{# Ce template n'hérite de personne, tout comme le template inclus avec {% include %}. #}
<ul>
<ul class="nav nav-pills nav-stacked">
{% for article in liste_articles %}
<li><a href="{{ path('sdzblog_voir', {'id': article.id}) }}">{{
article.titre }}</a></li> {% endfor %}
</ul> </ul>